rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false; // Arquitetura segura de proxy: todo o acesso é mediado e auditado pela API do servidor
    }
  }
}
